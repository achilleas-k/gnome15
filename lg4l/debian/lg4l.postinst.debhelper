# Automatically added by dh_installudev
if [ "$1" = configure ]; then
	if [ -e "/etc/udev/rules.d/z60_lg4l.rules" ]; then
		echo "Preserving user changes to /etc/udev/rules.d/40-lg4l.rules ..."
		if [ -e "/etc/udev/rules.d/40-lg4l.rules" ]; then
			mv -f "/etc/udev/rules.d/40-lg4l.rules" "/etc/udev/rules.d/40-lg4l.rules.dpkg-new"
		fi
		mv -f "/etc/udev/rules.d/z60_lg4l.rules" "/etc/udev/rules.d/40-lg4l.rules"
	fi
fi
# End automatically added section
